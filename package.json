{
  "name": "delay-repeater",
  "version": "1.0.1",
  "description": "Run a function repeatly with a delay",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "npm run build && mocha -R spec --compilers js:babel-register test/*.test.js"
  },
  "author": "xavierchow",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-register": "^6.18.0",
    "chai": "^3.5.0",
    "sinon": "^1.17.6"
  }
}
